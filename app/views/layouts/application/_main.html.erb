<div class="flex flex-col md:flex-row flex-grow">
  <%= render 'layouts/application/sidebar' %>
  <main class="flex-grow p-4 order-2 bg-orange-50">
    <p class="notice bg-green-20 text-green-500"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </main>
</div>
